#!/usr/bin/python3

def square_matrix_simple(matrix=[]):
    #  a function that computes the square value of all integers of a matrix.
    new_matrix = [list(map(lambda n: n ** 2, row)) for row in matrix]

    return new_matrix

    for row in matrix:
        return (row)
