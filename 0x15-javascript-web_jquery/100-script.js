// Updates the text color of the <header> element to red using no jQuery, the script is imported from the <head> tag, not at the end of the HTML.
document.addEventListener('DOMContentLoaded', function () {
  document.querySelector('header').style.color = '#FF0000';
});
